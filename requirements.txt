scapy==2.5.0
# Fase 2 (simulador SimPy). Mínimos com >= para usar wheels pré-compiladas
# em Python 3.14 (pins exatos antigos forçavam build do fonte e falhavam).
simpy>=4.1
pytest>=7.4
plotly>=5.18
numpy>=1.26
scipy>=1.12
# Exportar figuras Plotly para PNG. kaleido>=1.0 requer um Chrome headless:
# rode `plotly_get_chrome` uma vez (no Colab o Chrome já existe).
kaleido>=1.0
# Notebook local: nbformat é exigido pelo plotly `fig.show()` (renderização mime);
# ipykernel registra o kernel do .venv no Jupyter/VS Code. No Colab ambos já existem.
nbformat>=5.0
ipykernel>=6.0
